# Recommendation 代码样例

本目录保留菜谱推荐相关的 C# 后端样例代码。

## 包含文件

- `RecommendedRecipesController.cs`：推荐菜谱 API 入口。
- `RecommendedRecipeService.cs`：推荐业务逻辑，包含偏好过滤、标签排序、搜索、向量相似度 fallback 和热门菜谱 fallback。

这些文件用于展示推荐系统在业务规则、用户偏好、饮食限制和语义检索之间的组合方式。
